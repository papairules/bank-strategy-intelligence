[project]
name = "bank-strategy-intelligence"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "pydantic-settings",
    "google-genai",
    "openai>=1.68.0",
    "langgraph>=0.3.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
]
